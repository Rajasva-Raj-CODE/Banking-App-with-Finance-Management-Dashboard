'use server'

export const signIn = async ()=>{
    try {
        //Mutation/Database/API call
    } catch (error) {
        console.error('Error signing in:', error);
    }
}

export const signUp = async (userData:SignUpParams)=>{
    try {
        //Mutation/Database/API call
    } catch (error) {
        console.error('Error signing in:', error);
    }
}